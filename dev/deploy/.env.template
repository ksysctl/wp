# WP Project settings

# MySQL

DB_CONTAINER=db_mysql
DB_PORT=3306
DB_DATA=../../data/db
DB_SQL=../../data/sql
DB_HOST=db
DB_PASSWORD=cms
DB_USERNAME=cms
DB_NAME=cms
DB_ROOT_PASSWORD=root

# Wordpress

# - if WP_DEV_MODE is DEV then it enable debug settings otherwise not
# - WP unique keys and salts, generate them from
# https://api.wordpress.org/secret-key/1.1/salt/

WP_CONTAINER=wp_webserver
WP_PORT=80
WP_ROOT=../../wordpress
WP_DOCKER=dev/build/Dockerfile
WP_DEV_MODE=DEV
WP_USERNAME=cms
WP_PASSWORD=cms
WP_PROJECT=WP Project
WP_LIVE_SERVER=cms.com
WP_LOCAL_SERVER=cms.loc
WP_LIVE_URL=https://cms.com
WP_LOCAL_URL=http://cms.loc

WP_AUTH_KEY=
WP_SECURE_AUTH_KEY=
WP_LOGGED_IN_KEY=
WP_NONCE_KEY=
WP_AUTH_SALT=
WP_SECURE_AUTH_SALT=
WP_LOGGED_IN_SALT=
WP_NONCE_SALT=

# General configs

PHP_CONFIG=../../dev/deploy/php.ini
MYSQL_VERSION=5.7